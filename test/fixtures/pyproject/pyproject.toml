[project]
name = "my-project"
version = "1.0.0"

dependencies = [
    "requests>=2.28.0",
    "flask[async]>=2.0",
    "django~=4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "black",
]
test = [
    "coverage",
]
